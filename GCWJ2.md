# 计算器历史记录功能说明文档

## 功能概述

本次更新为计算器应用添加了完整的历史记录功能，用户可以查看、恢复之前的计算记录，提升使用体验。

## 新增功能详情

### 1. 历史记录自动保存
- **触发时机**：每次按下等号（=）完成计算后
- **保存内容**：
  - 原始计算表达式（如：`1+2+3`）
  - 计算结果（如：`6`）
  - 时间戳（用于排序和未来扩展）
- **存储限制**：最多保存20条历史记录，超出后自动删除最早的记录
- **存储方式**：使用内存存储，应用关闭后数据会清空

### 2. 历史记录查看
- **入口**：点击显示区域右侧的"历史"按钮
- **展示方式**：
  - 可展开/折叠的历史记录面板
  - 列表形式展示，支持滚动查看
  - 奇偶行使用不同背景色，提升可读性
- **信息展示**：
  - 第一行：原始表达式 + "="
  - 第二行：计算结果（加粗显示）

### 3. 历史记录恢复
- **操作方式**：点击历史记录列表中的任意一项
- **恢复行为**：
  - 将该记录的计算结果恢复到输入框
  - 自动关闭历史记录面板
  - 用户可基于该结果继续计算

### 4. 历史记录清除
- **清除按钮**：位于历史记录面板右上角
- **按钮样式**：红色背景，白色文字
- **清除行为**：一键清空所有历史记录
- **提示信息**：无历史记录时显示"暂无历史记录"

## 技术实现

### 数据结构
```typescript
interface HistoryItem {
  expression: string;  // 计算表达式
  result: string;      // 计算结果
  timestamp: number;   // 时间戳
}
```

### 状态管理
```typescript
@State history: Array<HistoryItem> = [];      // 历史记录数组
@State showHistory: boolean = false;          // 历史记录面板显示状态
```

### 核心方法
1. **addToHistory(expression: string, result: string)**
   - 添加新的历史记录项
   - 使用 `unshift` 将新记录插入数组开头
   - 超过20条时自动删除最后一条

2. **clearHistory()**
   - 清空历史记录数组

3. **restoreFromHistory(item: HistoryItem)**
   - 从历史记录恢复表达式
   - 关闭历史记录面板

### UI组件结构
```
Column (主容器)
├── Column (显示区域)
│   ├── Row
│   │   ├── Text (表达式显示)
│   │   └── Button (历史按钮)
├── if (showHistory)
│   └── Column (历史记录面板)
│       ├── Row (标题栏)
│       │   ├── Text (标题)
│       │   └── Button (清除按钮)
│       └── List (历史记录列表)
│           └── ForEach (历史记录项)
```

## 修改的文件

### 1. entry/src/main/ets/pages/Index.ets
- 添加历史记录相关状态变量
- 修改 `onInputValue` 方法，在计算完成时保存历史
- 添加历史记录管理方法
- 重构 `build` 方法，添加历史记录UI组件

### 2. entry/src/main/ets/calc/pages/CardCalc.ets
- 与 Index.ets 保持一致的修改
- 确保两个页面功能同步

## 用户操作指南

### 查看历史记录
1. 进行一次或多次计算操作
2. 点击显示区域右侧的"历史"按钮
3. 历史记录面板将展开显示

### 使用历史记录
1. 打开历史记录面板
2. 点击想要使用的历史记录项
3. 该记录的结果将自动填入输入框
4. 历史记录面板自动关闭
5. 可继续基于该结果进行计算

### 清除历史记录
1. 打开历史记录面板
2. 点击右上角的"清除"按钮
3. 所有历史记录将被清空
4. 显示"暂无历史记录"提示

## 界面设计说明

### 颜色方案
- **历史按钮**：浅蓝色背景（#33007DFF），蓝色文字（#007DFF）
- **清除按钮**：红色背景（#FF0000），白色文字
- **历史面板背景**：浅灰色（#F0F0F0）
- **列表项背景**：交替使用白色和浅灰色（#F5F5F5）
- **表达式文字**：深灰色（#666666）
- **结果文字**：深色（#333333），加粗显示

### 尺寸规格
- **历史按钮**：宽60，高35
- **历史面板高度**：占总高度的30%
- **列表项内边距**：10
- **圆角**：面板底部圆角20，列表项圆角8

## 未来扩展建议

1. **持久化存储**
   - 使用 Preferences 或数据库保存历史记录
   - 应用重启后仍可查看历史

2. **更多操作**
   - 长按删除单条记录
   - 复制表达式或结果到剪贴板

3. **分类筛选**
   - 按日期分组显示
   - 搜索历史记录

4. **数据统计**
   - 计算次数统计
   - 常用操作分析

## 版本信息

- **功能添加日期**：2026-05-19
- **影响范围**：主页面和卡片计算器页面
- **构建状态**：✅ 通过
